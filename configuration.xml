<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <editor default="wiki"/>
    <!-- 
        The root collection which should be scanned for wiki feeds.
        Relative paths will be resolved relative to the app root.
    -->
    <root>data</root>
    <!--
    	If a user element is present, only database users listed in an allow element can
    	log into the wiki.
    -->
    <!--users>
        <allow user="editor"/>
        <allow user="admin"/>
    </users-->
    <slideshows-enabled>true</slideshows-enabled>
</configuration>